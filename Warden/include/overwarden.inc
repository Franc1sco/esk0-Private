#if defined _overwarden_included_
  #endinput
#endif
#define _overwarden_included_
/**
 * Check if client is Warden
 *
 * @param client
 * @return        True if client is warden.
 */
native bool OW_IsClientWarden(int client);
